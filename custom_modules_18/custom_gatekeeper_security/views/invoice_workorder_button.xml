<odoo>
    <data>
        <!-- Work Order Button in Invoice Header -->
        <record id="view_move_form_work_order_button" model="ir.ui.view">
            <field name="name">account.move.form.work.order.button</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <button 
                            name="action_create_work_order"
                            string="Create Work Order"
                            type="object"
                            class="btn-primary"
                            invisible="move_type != 'out_invoice' or payment_state != 'paid' or state != 'posted'"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
