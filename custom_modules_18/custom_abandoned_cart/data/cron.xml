<odoo>
    <record id="ir_cron_whatsapp_abandoned_cart" model="ir.cron">
        <field name="name">WhatsApp Abandoned Cart</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">model._cron_send_abandoned_cart_whatsapp()</field>
        <field name="interval_number">30</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>
</odoo>