<odoo>
    <template id="dealer_signup_form" name="Dealer Signup Form">
        <t t-call="website.layout">
            <t t-set="title">Sign up as a Dealer</t>
            
            <div class="container mt-5">
                <div class="row justify-content-center">
                    <div class="col-md-5 col-lg-4">
                        <div class="card shadow-sm">
                            <div class="card-body p-4">
                                <h2 class="text-center mb-4" style="color: #6B46C1; font-weight: 600;">Sign up</h2>
                                
                                <t t-if="errors and errors.get('general')">
                                    <div class="alert alert-danger" role="alert">
                                        <t t-esc="errors.get('general')"/>
                                    </div>
                                </t>

                                <form method="post" action="/dealers/signup/submit" id="dealer_signup_form">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    
                                    <!-- Name Field -->
                                    <div class="mb-3">
                                        <label for="name" class="form-label" style="color: #374151; font-weight: 500;">Name</label>
                                        <input type="text" 
                                               class="form-control" 
                                               id="name" 
                                               name="name" 
                                               placeholder="John Doe"
                                               t-att-value="name or ''"
                                               required="required"
                                               t-att-class="'form-control is-invalid' if errors and errors.get('name') else 'form-control'"/>
                                        <t t-if="errors and errors.get('name')">
                                            <div class="invalid-feedback d-block">
                                                <t t-esc="errors.get('name')"/>
                                            </div>
                                        </t>
                                    </div>

                                    <!-- Email Field -->
                                    <div class="mb-3">
                                        <label for="email" class="form-label" style="color: #374151; font-weight: 500;">Email</label>
                                        <input type="email" 
                                               class="form-control" 
                                               id="email" 
                                               name="email" 
                                               placeholder="name@example.com"
                                               t-att-value="email or ''"
                                               required="required"
                                               t-att-class="'form-control is-invalid' if errors and errors.get('email') else 'form-control'"/>
                                        <t t-if="errors and errors.get('email')">
                                            <div class="invalid-feedback d-block">
                                                <t t-esc="errors.get('email')"/>
                                            </div>
                                        </t>
                                    </div>

                                    <!-- Password Field -->
                                    <div class="mb-3">
                                        <label for="password" class="form-label" style="color: #374151; font-weight: 500;">Password</label>
                                        <div class="input-group">
                                            <input type="password" 
                                                   class="form-control" 
                                                   id="password" 
                                                   name="password" 
                                                   placeholder="Enter password"
                                                   required="required"
                                                   t-att-class="'form-control is-invalid' if errors and errors.get('password') else 'form-control'"/>
                                            <button class="btn btn-outline-secondary" type="button" id="togglePassword" style="border-color: #D1D5DB;">
                                                <i class="fa fa-eye" id="passwordIcon"></i>
                                            </button>
                                        </div>
                                        <t t-if="errors and errors.get('password')">
                                            <div class="invalid-feedback d-block">
                                                <t t-esc="errors.get('password')"/>
                                            </div>
                                        </t>
                                    </div>

                                    <!-- Confirm Password Field -->
                                    <div class="mb-4">
                                        <label for="confirm_password" class="form-label" style="color: #374151; font-weight: 500;">Confirm Password</label>
                                        <div class="input-group">
                                            <input type="password" 
                                                   class="form-control" 
                                                   id="confirm_password" 
                                                   name="confirm_password" 
                                                   placeholder="Confirm password"
                                                   required="required"
                                                   t-att-class="'form-control is-invalid' if errors and errors.get('confirm_password') else 'form-control'"/>
                                            <button class="btn btn-outline-secondary" type="button" id="toggleConfirmPassword" style="border-color: #D1D5DB;">
                                                <i class="fa fa-eye" id="confirmPasswordIcon"></i>
                                            </button>
                                        </div>
                                        <t t-if="errors and errors.get('confirm_password')">
                                            <div class="invalid-feedback d-block">
                                                <t t-esc="errors.get('confirm_password')"/>
                                            </div>
                                        </t>
                                    </div>

                                    <!-- Submit Button -->
                                    <button type="submit" class="btn w-100" style="background-color: #6B46C1; color: white; border-radius: 8px; padding: 12px; font-weight: 500;">
                                        Sign up
                                    </button>
                                </form>

                                <!-- Login Link -->
                                <div class="text-center mt-4">
                                    <p style="color: #6B46C1; font-size: 14px;">
                                        Already have an account? <a href="/web/login" style="color: #6B46C1; text-decoration: none; font-weight: 500;">Sign in</a>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <script><![CDATA[
                // Password visibility toggle
                document.addEventListener('DOMContentLoaded', function() {
                    const togglePassword = document.getElementById('togglePassword');
                    const passwordInput = document.getElementById('password');
                    const passwordIcon = document.getElementById('passwordIcon');
                    
                    const toggleConfirmPassword = document.getElementById('toggleConfirmPassword');
                    const confirmPasswordInput = document.getElementById('confirm_password');
                    const confirmPasswordIcon = document.getElementById('confirmPasswordIcon');

                    if (togglePassword && passwordInput) {
                        togglePassword.addEventListener('click', function() {
                            const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
                            passwordInput.setAttribute('type', type);
                            passwordIcon.classList.toggle('fa-eye');
                            passwordIcon.classList.toggle('fa-eye-slash');
                        });
                    }

                    if (toggleConfirmPassword && confirmPasswordInput) {
                        toggleConfirmPassword.addEventListener('click', function() {
                            const type = confirmPasswordInput.getAttribute('type') === 'password' ? 'text' : 'password';
                            confirmPasswordInput.setAttribute('type', type);
                            confirmPasswordIcon.classList.toggle('fa-eye');
                            confirmPasswordIcon.classList.toggle('fa-eye-slash');
                        });
                    }
                });
            ]]></script>
        </t>
    </template>
</odoo>
